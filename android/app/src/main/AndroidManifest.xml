<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Spec section 11: microphone -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- AudioManager.setMode() is gated on this one: without it AudioService's
         checkAudioSettingsPermission() makes the call a silent no-op, and the radio never
         gets the shared echo-cancelled voice route or the call-stream volume keys it asks
         for in RadioForegroundService.setCommunicationMode(). Install-time, so nothing has
         to request it at runtime. -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Spec section 11: Nearby Connections + the external PTT button -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- Spec section 11: Nearby discovery -->
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />
    <!-- Unconditional on every API level, not capped at 32: Nearby Connections' BLE medium
         (play-services-nearby 19.4.0) performs its own internal permission check ahead of
         startAdvertising()/startDiscovery() that requires ACCESS_FINE_LOCATION (or COARSE) to
         be granted, and throws ConnectionsStatusCodes 8034
         MISSING_PERMISSION_ACCESS_COARSE_LOCATION when it is not; observed for real on an
         Android 16 (API 36) device with NEARBY_WIFI_DEVICES granted and no location permission
         at all. That check is internal to the library, not the OS's BLUETOOTH_SCAN enforcement,
         so android:usesPermissionFlags="neverForLocation" on BLUETOOTH_SCAN below cannot
         suppress it: that flag only changes what the OS/PackageManager require, and this is a
         library-thrown status code, not a SecurityException. NEARBY_WIFI_DEVICES is kept for
         the Wi-Fi-based mediums; capping ACCESS_FINE_LOCATION below 33 was the actual bug (Bug
         found #3, docs/superpowers/specs/2026-08-13-phase0-spike-report.md). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Bug #5, docs/superpowers/specs/2026-08-13-phase0-spike-report.md: with only the
         foreground-scoped grant above, Android's location appops enforcement stops counting the
         app as "foreground" a few minutes after its last visible Activity goes away (this radio's
         foreground service has no "location" foregroundServiceType, so it doesn't count either),
         and Nearby Connections' ClientProxy then silently withholds onEndpointFound() forever,
         confirmed via adb shell appops get with no error surfaced anywhere. ACCESS_BACKGROUND_LOCATION
         removes that foreground gate entirely. Needs its own runtime "Allow all the time" consent
         step and a Play Store Data Safety disclosure, both still open and owned by P7's onboarding
         work, not done here. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Nearby Connections' Wi-Fi-based mediums (advertising/discovery) need these; without
         them the engine fails immediately on start with advertising_failed/discovery_failed
         MISSING_PERMISSION_ACCESS_WIFI_STATE. -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- Spec section 11: foreground service -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
        <service
            android:name="com.oru.radio.RadioForegroundService"
            android:exported="false"
            android:foregroundServiceType="microphone|connectedDevice" />
    </application>
</manifest>
